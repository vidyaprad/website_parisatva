# Node 12 is too old for react-scripts 5's ESLint config (uses the `node:` import protocol, needs Node 16+).
# Disable the ESLint webpack plugin so the dev server / build don't crash on the unsupported config.
DISABLE_ESLINT_PLUGIN=true
